#!/usr/bin/python2.7
import threading
import time
import Queue
from scapy.all import *


#def customAction(self, packet):
#	print packet.summary()
#	if packet[0][1].src in self.sniffq:
#		print "Port 80 open at " + packet[0][1].src
#		del self.sniffq[self.sniffq.index(packet[0][1].src)]

print sniff(count=50)
rekt=_
print rekt.nsummary()
